/**
 * Campaign Creation Wizard screen component
 *
 * Guides the user through the process of creating a new campaign.
 * Includes steps for selecting modules, narrator style, rules, and character setup.
 */
export function Wizard() {
  return (
    <div className="wizard-screen">
      <h1>Create New Campaign</h1>
      <p>Campaign creation wizard - Select modules, narrator, rules, and character setup</p>
    </div>
  );
}
