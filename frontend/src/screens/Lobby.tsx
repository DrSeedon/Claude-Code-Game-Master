/**
 * Lobby screen component
 *
 * Displays the campaign list and allows users to create new campaigns
 * or join existing ones. This is the entry point of the application.
 */
export function Lobby() {
  return (
    <div className="lobby-screen">
      <h1>DM Game Master - Campaign Lobby</h1>
      <p>Select a campaign or create a new one</p>
    </div>
  );
}
