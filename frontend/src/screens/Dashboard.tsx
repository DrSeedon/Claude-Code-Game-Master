/**
 * Campaign Dashboard screen component
 *
 * Provides campaign management interface with character stats, wiki browser,
 * and map viewer. Allows DM to manage campaign settings and view game state.
 */
export function Dashboard() {
  return (
    <div className="dashboard-screen">
      <h1>Campaign Dashboard</h1>
      <p>Campaign management - Character stats, wiki browser, and map viewer</p>
    </div>
  );
}
